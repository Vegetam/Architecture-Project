# Values for order-service
image:
  repository: ghcr.io/vegetam/order-service
  tag: latest

service:
  port: 3001

rollout:
  enabled: true
  strategy: canary

serviceMonitor:
  enabled: true
  path: /metrics
